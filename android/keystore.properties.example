# Copy this file to keystore.properties and fill in real values.
# Only this example file should be stored in git.
# keystore.properties and the real keystore file are ignored and must stay local.

# Absolute or Android-root-relative path to your release keystore file.
storeFile=/absolute/path/to/ivi-release.jks

# Password for the keystore.
storePassword=change_me

# Alias of the signing key inside the keystore.
keyAlias=ivi

# Password for the signing key.
# If omitted in keystore.properties, Gradle will reuse storePassword.
keyPassword=change_me
